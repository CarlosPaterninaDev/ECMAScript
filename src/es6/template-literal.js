/* TODO: Template Literal : "TemplateElement" */

let hello = "Hello";
let world = "World";

// Antes
let epicPhrase = hello + " " + world;

// Ahora
let epicPhaseES6 = `${hello} ${world}`;

console.log(epicPhrase);
console.log(epicPhaseES6);
